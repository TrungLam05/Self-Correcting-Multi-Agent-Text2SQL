# Keep tests self-contained: avoid relying on main application.yaml being on the test classpath.
# Also prevent Spring SQL init from attempting DB connections during context startup.
spring.sql.init.mode=never

text2sql.db.jdbcUrl=jdbc:postgresql://localhost:5432/analytics
text2sql.db.username=app_readonly
text2sql.db.password=app_readonly_password